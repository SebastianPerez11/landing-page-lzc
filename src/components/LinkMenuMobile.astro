---
const { name, href } = Astro.props;
---

<a
  href={href}
  class=`rounded-sm font-semibold py-[6px] border-transparent transition-all w-full px-4 ${Astro.props.class}`
  title={name}>{name}</a
>
