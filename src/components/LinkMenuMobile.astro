---
const { name, href } = Astro.props;
---

<a
  href={href}
  class=`rounded-sm font-semibold py-2 border-transparent transition-all w-full px-4 ${Astro.props.class} linksMobile`
  title={name}>{name}</a
>
